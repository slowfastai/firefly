{
  "name": "@app/backend",
  "private": true,
  "scripts": {
    "dev": "python src/run_backend.py",
    "build": "echo '(python backend has no build step)'",
    "lint": "ruff check . && black --check .",
    "format": "ruff check . --fix && black ."
  }
}
